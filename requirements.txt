# Binance connectivity
python-binance
websockets

# Database
redis
psycopg2-binary
pgvector

# ML / Deep Learning
torch
pytorch-forecasting
torch-geometric
transformers
hmmlearn
ruptures
deap
stable-baselines3
scikit-learn
scipy
numpy
pandas
xgboost

# NOTE: airllm removed — replaced by llama.cpp GGUF (see TASKS.md H-04)
# NOTE: finrl removed — FinRL has complex deps; MARL uses stable-baselines3 directly

# cont. 54 — P2 Chronos-2 foundation forecaster (zero-shot 1h forecasts).
# Auto-defers to F19 TFT once trusted; cold-start fallback otherwise.
# ml/foundation_forecast.py is cold-start-safe: returns None when the
# package isn't importable, so the install failing won't break the bot.
chronos-forecasting
huggingface_hub

# cont. 54 — P1 Mamba SSM forecaster runs on PURE PyTorch (no mamba-ssm
# CUDA kernel dependency). Implementation is in ml/mamba_forecaster.py.

# Web framework & API
fastapi
uvicorn[standard]
python-jose[cryptography]
passlib[bcrypt]
pydantic
slowapi                      # rate limiting for dashboard login endpoint

# Async HTTP (Ollama calls, watchdog health checks)
aiohttp
httpx

# Task queue
celery[redis]

# Notifications
python-telegram-bot

# Web intelligence
praw
feedparser
requests
beautifulsoup4
google-search-results        # SerpAPI client (PyPI package name, imported as serpapi)

# Config & utilities
pyyaml
python-dotenv
structlog

# F49 Autonomous Self-Training Orchestrator (cont. 47) — Bayesian HPO
optuna

# Testing
pytest
pytest-asyncio
docker
hdbscan>=0.8.33  # Phase A cont. 55 — online cluster prediction
